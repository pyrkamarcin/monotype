{% extends 'base.html.twig' %}
{% form_theme form 'form/fields.html.twig' %}

{% block body %}
    <h1>User creation</h1>

    {% block form_row %}
        {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create"/>
        {{ form_end(form) }}

    {% endblock %}

    <ul>
        <li>
            <a href="{{ path('manager_user_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
