{% extends 'base.html.twig' %}

{% block body %}
    <h1>WorkerCategory</h1>

    <table>
        <tbody>
        <tr>
            <th>Id</th>
            <td>{{ workerCategory.id }}</td>
        </tr>
        <tr>
            <th>Fullname</th>
            <td>{{ workerCategory.fullName }}</td>
        </tr>
        <tr>
            <th>Name</th>
            <td>{{ workerCategory.name }}</td>
        </tr>
        <tr>
            <th>Description</th>
            <td>{{ workerCategory.description }}</td>
        </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('manager_workercategory_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('manager_workercategory_edit', { 'id': workerCategory.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
            <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
